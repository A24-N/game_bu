<div class="container">
  <div class="row">
    <div class="col-11 offset-1">
      <h2 class="heading mt-4"><%= @user.nickname %>さんのフォロワー一覧</h2>
    </div>
    <div class="col-xl-3 col-sm-11 side m-2">
      <%= render 'users/info', user: current_user %>
    </div>
    <div class="col-xl-8 col-sm-11">
      <%= render 'follow', users: @users %>
      <%= paginate @users %>
    </div>
  </div>
</div>