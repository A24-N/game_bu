<div class="container mt-4">
  <div class="row">
    <%= render 'users/info', user: current_user %>

    <div class="col-8">
      <p class="h4">投稿一覧</p>
      <div class="col-8 offset-1">
        <p>今の近況を投稿しよう！</p>
        <%= render 'form', npost: @npost %>
      </div>

      <div class="mt-4">
        <%= render 'index', posts: @posts %>
      </div>

    </div>
  </div>
</div>